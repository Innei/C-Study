#include <stdio.h>

int main(int argc, char const *argv[])
{
    int in = 0;
    printf("enter a sum \n");
    scanf("%d", &in);
    for (int i = 2; i <= n; i++)
    {
        for (int k = i + 1; k < i; k++)
        {
            if (i % k != 0)
            {
                continue;
            }
            else
            {
                break;
            }
            printf("%d\n", i);
        }
    }
    return 0;
}
