#include <stdio.h>
#include <ctype.h>
#include <string.h>
int main(int argc, char const *argv[])
{
    static char a[80];
    scanf("%s",a);
    int len = strlen(a);
    for(int i = 0; i < len; i++)
    {
        
        if (is) {
            /* code */
        }
        
    }
    
    return 0;
}
